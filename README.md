# Vituum Path Resolution Problem

```shell
npm run dev
npm run build
npm run preview
```

> [!CAUTION]  
> At this point only `dev` is working, `build` ends with error:

![](docs/vite-build-error.png)
